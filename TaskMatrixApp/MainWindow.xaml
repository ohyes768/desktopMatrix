<Window x:Class="TaskMatrixApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TaskMatrix - 任务矩阵" Height="380" Width="280"
        WindowStyle="None"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="Manual"
        Left="{Binding WindowPosition.X, Mode=TwoWay}"
        Top="{Binding WindowPosition.Y, Mode=TwoWay}"
        MouseLeftButtonDown="TitleBar_MouseDown">

    <Window.Resources>
        <!-- 象限颜色 - 淡色版本（主界面使用） -->
        <SolidColorBrush x:Key="Q1Brush" Color="#60FFEBEE"/>
        <SolidColorBrush x:Key="Q2Brush" Color="#60E3F2FD"/>
        <SolidColorBrush x:Key="Q3Brush" Color="#60FFF3E0"/>
        <SolidColorBrush x:Key="Q4Brush" Color="#60E8F5E9"/>

        <!-- 象限选择器颜色 - 深色版本（按钮使用） -->
        <SolidColorBrush x:Key="Q1ButtonBrush" Color="#E53935"/>
        <SolidColorBrush x:Key="Q2ButtonBrush" Color="#1976D2"/>
        <SolidColorBrush x:Key="Q3ButtonBrush" Color="#FB8C00"/>
        <SolidColorBrush x:Key="Q4ButtonBrush" Color="#43A047"/>

        <!-- 象限选择器高亮颜色 -->
        <SolidColorBrush x:Key="Q1HighlightBrush" Color="#B71C1C"/>
        <SolidColorBrush x:Key="Q2HighlightBrush" Color="#0A3D91"/>
        <SolidColorBrush x:Key="Q3HighlightBrush" Color="#E65100"/>
        <SolidColorBrush x:Key="Q4HighlightBrush" Color="#1B5E20"/>

        <!-- 选中状态边框颜色 -->
        <SolidColorBrush x:Key="SelectedBorderBrush" Color="White"/>
        <SolidColorBrush x:Key="UnselectedBorderBrush" Color="#20000000"/>

        <!-- 标题栏样式 -->
        <Style x:Key="TitleBarStyle" TargetType="Border">
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="CornerRadius" Value="8,8,0,0"/>
        </Style>

        <!-- 象限边框样式 -->
        <Style x:Key="QuadrantBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <!-- 象限标题样式 -->
        <Style x:Key="QuadrantTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- 任务卡片样式 -->
        <Style x:Key="TaskCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#D0FFFFFF"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 象限选择按钮默认样式 -->
        <Style x:Key="QuadrantButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 象限选择按钮选中状态样式 -->
        <Style x:Key="QuadrantButtonSelectedStyle" TargetType="Button" BasedOn="{StaticResource QuadrantButtonStyle}">
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#2C3E50" BlurRadius="6" ShadowDepth="2" Opacity="0.6"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 快速按钮样式 -->
        <Style x:Key="QuickButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="8,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
        </Style>
    </Window.Resources>

    <Border Background="#B0FFFFFF" CornerRadius="8" BorderBrush="#90909090" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Style="{StaticResource TitleBarStyle}" Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>

                    <!-- 标题文字 -->
                    <TextBlock Grid.Column="0" Text="TaskMatrix"
                               Foreground="White" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="10,0,0,0"
                               FontSize="12"/>

                    <!-- 关闭按钮 -->
                    <Button Grid.Column="1" Content="×"
                            Background="Transparent" Foreground="White"
                            BorderThickness="0" FontSize="14" FontWeight="Bold"
                            Cursor="Hand" Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <!-- 主内容区 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧：四个象限 -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140"/>
                        <RowDefinition Height="140"/>
                    </Grid.RowDefinitions>

                    <!-- Q1: 重要紧急 -->
                    <Border Grid.Row="0" Style="{StaticResource QuadrantBorder}" Background="{StaticResource Q1Brush}">
                        <StackPanel>
                            <TextBlock Text="🔥 重要紧急" Style="{StaticResource QuadrantTitle}" Foreground="#D32F2F"/>
                            <ScrollViewer Height="95" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Q1Tasks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource TaskCardStyle}">
                                                <CheckBox Content="{Binding Title}"
                                                         IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                         FontSize="9"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Q3: 紧急不重要 -->
                    <Border Grid.Row="1" Style="{StaticResource QuadrantBorder}" Background="{StaticResource Q3Brush}">
                        <StackPanel>
                            <TextBlock Text="⚡ 紧急不重要" Style="{StaticResource QuadrantTitle}" Foreground="#FF9800"/>
                            <ScrollViewer Height="95" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Q3Tasks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource TaskCardStyle}">
                                                <CheckBox Content="{Binding Title}"
                                                         IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                         FontSize="9"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 右侧：Q2、Q4和快速操作 -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140"/>
                        <RowDefinition Height="140"/>
                        <RowDefinition Height="80"/>
                    </Grid.RowDefinitions>

                    <!-- Q2: 重要不紧急 -->
                    <Border Grid.Row="0" Style="{StaticResource QuadrantBorder}" Background="{StaticResource Q2Brush}">
                        <StackPanel>
                            <TextBlock Text="💡 重要不紧急" Style="{StaticResource QuadrantTitle}" Foreground="#1976D2"/>
                            <ScrollViewer Height="95" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Q2Tasks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource TaskCardStyle}">
                                                <CheckBox Content="{Binding Title}"
                                                         IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                         FontSize="9"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Q4: 不重要不紧急 -->
                    <Border Grid.Row="1" Style="{StaticResource QuadrantBorder}" Background="{StaticResource Q4Brush}">
                        <StackPanel>
                            <TextBlock Text="🌱 不重要不紧急" Style="{StaticResource QuadrantTitle}" Foreground="#4CAF50"/>
                            <ScrollViewer Height="95" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Q4Tasks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource TaskCardStyle}">
                                                <CheckBox Content="{Binding Title}"
                                                         IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                         FontSize="9"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- 快速操作区 -->
                    <Grid Grid.Row="2" Margin="8,8,8,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="添加"
                                Style="{StaticResource QuickButtonStyle}"
                                Margin="0,0,4,0"
                                Click="AddTaskCommand_Executed"/>

                        <Button Grid.Column="1" Content="清理"
                                Style="{StaticResource QuickButtonStyle}"
                                Margin="4,0,0,0"
                                Click="CleanupButton_Click"/>
                    </Grid>
                </Grid>
            </Grid>

            <!-- 添加任务对话框（初始隐藏） -->
            <Grid x:Name="AddTaskPanel" Grid.Row="0" Grid.RowSpan="2"
                  Background="#F8F8F8" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="#E0FFFFFF" CornerRadius="8" Margin="20" BorderBrush="#A0A0A0A0" BorderThickness="1">
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 标题 -->
                        <TextBlock Grid.Row="0" Text="添加新任务" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

                        <!-- 任务标题输入 -->
                        <TextBox x:Name="TaskTitleTextBox"
                                 Grid.Row="1"
                                 Height="30"
                                 FontSize="12"
                                 Padding="5"
                                 BorderBrush="#D0D0D0"
                                 BorderThickness="1"/>

                        <!-- 象限选择 -->
                        <StackPanel Grid.Row="2" Margin="0,15">
                            <TextBlock Text="选择象限:" FontWeight="Bold" FontSize="11"/>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border x:Name="Q1Border" Grid.Column="0" Grid.Row="0" CornerRadius="8" Margin="0,0,2,2"
                                    Background="{StaticResource Q1ButtonBrush}" BorderThickness="2"
                                    BorderBrush="{StaticResource UnselectedBorderBrush}">
                                <Button x:Name="Q1Button" Content="重要紧急"
                                        Background="Transparent" BorderThickness="0"
                                        Padding="10,8" FontSize="11" Foreground="White"
                                        FontWeight="Medium" Cursor="Hand"
                                        Click="SelectQuadrantButton_Click"/>
                            </Border>
                            <Border x:Name="Q2Border" Grid.Column="1" Grid.Row="0" CornerRadius="8" Margin="0,0,0,2"
                                    Background="{StaticResource Q2ButtonBrush}" BorderThickness="2"
                                    BorderBrush="{StaticResource UnselectedBorderBrush}">
                                <Button x:Name="Q2Button" Content="重要不紧急"
                                        Background="Transparent" BorderThickness="0"
                                        Padding="10,8" FontSize="11" Foreground="White"
                                        FontWeight="Medium" Cursor="Hand"
                                        Click="SelectQuadrantButton_Click"/>
                            </Border>
                            <Border x:Name="Q3Border" Grid.Column="0" Grid.Row="1" CornerRadius="8" Margin="0,2,2,0"
                                    Background="{StaticResource Q3ButtonBrush}" BorderThickness="2"
                                    BorderBrush="{StaticResource UnselectedBorderBrush}">
                                <Button x:Name="Q3Button" Content="紧急不重要"
                                        Background="Transparent" BorderThickness="0"
                                        Padding="10,8" FontSize="11" Foreground="White"
                                        FontWeight="Medium" Cursor="Hand"
                                        Click="SelectQuadrantButton_Click"/>
                            </Border>
                            <Border x:Name="Q4Border" Grid.Column="1" Grid.Row="1" CornerRadius="8" Margin="0,2,0,0"
                                    Background="{StaticResource Q4ButtonBrush}" BorderThickness="2"
                                    BorderBrush="{StaticResource UnselectedBorderBrush}">
                                <Button x:Name="Q4Button" Content="不重要不紧急"
                                        Background="Transparent" BorderThickness="0"
                                        Padding="10,8" FontSize="11" Foreground="White"
                                        FontWeight="Medium" Cursor="Hand"
                                        Click="SelectQuadrantButton_Click"/>
                            </Border>
                            </Grid>
                        </StackPanel>

                        <!-- 按钮 -->
                        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
                            <Button Content="添加" Width="60" Height="25" Margin="0,0,10,0"
                                    Background="#3498DB" Foreground="White" BorderThickness="0"
                                    Click="ConfirmAddTask_Click"/>
                            <Button Content="取消" Width="60" Height="25"
                                    Background="#95A5A6" Foreground="White" BorderThickness="0"
                                    Click="CancelAddTask_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>